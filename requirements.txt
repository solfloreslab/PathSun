# PathSun — requirements.txt curado para Streamlit Community Cloud.
# Versiones de major intencionalmente flexibles para evitar fallos de
# resolucion en el entorno de Streamlit Cloud (Python 3.11 via runtime.txt).
#
# Para reproduccion exacta del entorno local, ver requirements_freeze.txt
# (no incluido en repo publico).

streamlit>=1.32,<2.0
tensorflow>=2.16,<2.20
keras>=3.7,<4.0
pandas>=2.0,<3.0
numpy>=1.24,<2.0
pillow>=10.0
opencv-python-headless>=4.8
